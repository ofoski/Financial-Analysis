[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "BLE", "S", "RUF", "PLW"]

[tool.ruff.lint.per-file-ignores]
# assert is how pytest tests are supposed to work, not a risk here.
"deployment/tests/*" = ["S101"]
